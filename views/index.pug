extends layout

block content
	.container
		h1 Welcome
		if (!user)
			a(href="/login") Login
			br
			a(href="/signup") Signup
		if (user)
			p You are currently logged in as #{user.email}
			a(href="/logout") Logout